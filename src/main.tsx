import React from 'react'
import ReactDOM from 'react-dom/client'
import SecondBlock from './secondBlock/secondBlock.tsx'
// import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
	<React.StrictMode>
		<SecondBlock />
	</React.StrictMode>
)
